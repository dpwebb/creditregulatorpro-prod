# Internal Dev Archive

This directory preserves historical planning notes, implementation notes, and scheduled-job drafts that used to live under `static/__dev`.

The application serves `static` as public assets, so internal planning material must stay outside that tree. Some archived files may still mention their old `static/__dev` paths because they are historical notes; those references are not active runtime paths.
