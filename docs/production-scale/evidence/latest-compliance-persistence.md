# Compliance Finding Persistence Evidence

Generated at: 2026-05-21T04:04:40.1692716-03:00

Current HEAD: `47bdb3fd883d2ee9ed19af035a82e1a76cf5d1ea`

Audit target: P1-5 Compliance findings can be deleted before replacement inserts succeed.

CERTIFYING:false

Reason: this evidence is automated local simulation and regression proof. It does not claim live production database concurrency certification.

## Implementation Summary

- Patched `helpers/complianceScanner.tsx` around the existing `persistViolations` path.
- Replaced delete-then-insert active auto finding replacement with a single transaction.
- Added a tradeline `forUpdate()` row lock so same-tradeline concurrent scans serialize before replacement.
- Kept manual/admin findings by continuing to delete only active `autoGenerated = true` rows.
- Kept dismissed/verified/non-active findings outside the active auto replacement set.
- Preserved packet readiness logic and made it read only committed finding state by committing delete/insert as one transaction.
- Added small replacement metadata in `technicalDetails.complianceFindingReplacement`.

## Automated Evidence

- Simulated insert failure preserves previous active auto findings.
- Successful scan atomically replaces previous active auto findings.
- Manual/admin and non-active findings survive replacement.
- Packet-readiness-style reads do not observe the uncommitted partial set.
- Concurrent same-tradeline scan simulation does not produce mixed active truth.
- Existing packet readiness, packet template, packet PDF, and violation confidence gate tests pass.

## Commands Run

| Command | Result |
| --- | --- |
| `pnpm exec vitest run --config vitest.config.ts tests/unit/compliance-persistence-atomic.spec.ts` | PASS - 1 file, 6 tests |
| `pnpm exec tsc --noEmit --pretty false` | PASS |
| `pnpm exec vitest run --config vitest.config.ts tests/unit/compliance-persistence-atomic.spec.ts tests/unit/deterministic-ingestion-lockdown.spec.ts tests/unit/packet-readiness.spec.ts tests/unit/violation-packet-confidence-gate.spec.ts tests/unit/dispute-packet-template.spec.ts tests/unit/dispute-packet-pdf.spec.ts` | PASS - 6 files, 39 tests |
| `pnpm exec vitest run tests/unit tests/api --runInBand` | FAIL - Vitest 4.1.5 rejects unsupported option `--runInBand` before tests run |
| `pnpm exec vitest run --config vitest.config.ts tests/unit tests/api` | PASS - 187 files, 1418 tests |
| `git diff --check` | PASS |
| `pnpm run check` | PASS |

## Residual Risk

- Live production database contention was not exercised. The automated test uses an in-memory transaction and row-lock simulation, plus source checks for the Kysely transaction and `forUpdate()` lock.
